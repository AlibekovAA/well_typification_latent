# Типизация режимов нефтяных скважин на основе анализа латентных представлений

Система автоматического определения типовых режимов работы нефтяных скважин (ЭЦН и ШГН) с использованием автоэнкодеров и кластеризации.

## Структура проекта

```
code/
│
├── training/                   # ЧАСТЬ 1: Обучение
│   ├── eda.ipynb               # Этап 1: EDA + очистка + нормализация → data/processed/
│   ├── autoencoder.ipynb       # Этапы 2-3: окна → автоэнкодер → models/
│   ├── clustering.ipynb        # Этапы 4-6: кластеризация → типизация режимов скважин → models/
│
├── models/                     # архитектуры
│   ├── models.py
│
├── src/                        # ЧАСТЬ 2: Продакшн система (этапы 7-8)
│   ├── __init__.py
│   ├── config.py
│   └── utils.py
│   ├── streaming/              # Модуль потоковой обработки
│   │   ├── __init__.py
│   │   ├── emulator.py
│   │   ├── processor.py
│   │   └── storage.py
│   ├── dashboard/              # Модуль веб-интерфейса
│   │   ├── __init__.py
│   │   ├── app.py
│   │   └── components.py
│
├── main.py                     # Точка входа: запуск всей системы
```

## Данные

### Входные параметры (9 параметров влагомера)

1. Скорость ультразвука в центре трубы
2. Скорость ультразвука на периферии трубы
3. Газосодержание в центре трубы
4. Газосодержание на периферии трубы
5. Температура
6. Обводнённость в центре трубы
7. Обводнённость на периферии трубы
8. Интегральное газосодержание
9. Интегральная обводнённость

### Типы скважин и кластеры режимов

**ЭЦН (электроцентробежный насос, 4 кластера):**

| Кластер | Состояние                                 | Обоснование                                                                                                                                                                                                 |
| ------- | ----------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0       | Выключена / пуск                          | Активность минимальна, система стабильна; в продукции доминируют газовые признаки (gas_periph, gas_integral, gas_center), водяные и ультразвуковые компоненты выражены слабо — насос остановлен или запускается. |
| 1       | Переходный режим (разгон)                 | Активность возрастает до средней, система становится переменно-стабильной; появляются первые признаки воды и ультразвука, при этом газовые компоненты сохраняют значительную роль — характерный разгон насоса.   |
| 2       | Активная фаза работы на полной мощности   | Активность максимальная, система остаётся переменно-стабильной; водяные и ультразвуковые признаки доминируют, газовые компоненты присутствуют, но менее интенсивно — устоявшийся рабочий режим.                 |
| 3       | Заключительная высокоактивная стабильная фаза | Активность остаётся высокой, система стабилизируется; водяные и ультразвуковые признаки доминируют, газовые компоненты минимальны — финальная стадия цикла при устойчивой работе насоса.                        |

**ШГН (штанговый насос, 3 кластера):**

| Кластер | Состояние                                   | Обоснование                                                                                                                                                                                                     |
| ------- | ------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0       | Начальная газовая фаза                      | Активность минимальна, продукция характеризуется высоким содержанием газа и повышенной температурой; сигнал отличается значительной нестабильностью — стартовая, преимущественно газовая стадия цикла.        |
| 1       | Нефтяная фаза (умеренно-стабильная)         | Активность умеренная, система стабилизируется; преобладают водяные и ультразвуковые признаки, газовые компоненты снижаются — устойчивый участок цикла с доминированием жидкой фазы.                            |
| 2       | Переходная водо-газовая динамичная фаза     | Это наиболее динамичная стадия: резкое увеличение газовых компонентов, скорости ультразвука и общей нестабильности системы — выраженный водо-газовый переход в середине цикла.                                 |
| 3       | Завершающая высокоактивная стабильная фаза  | Доминируют водяные и ультразвуковые признаки, газовые компоненты минимальны; система функционирует в оптимальном режиме с максимальной производительностью — финальный, наиболее продуктивный участок цикла.   |

### Пороговые значения отклонения (thresholds.pkl)

Для каждого типа насоса и для каждого кластера в процессе кластеризации рассчитываются пороги отклонения латентного вектора от центра кластера по эмпирическому распределению расстояний:

- **mean** — среднее расстояние до центра кластера
- **std** — стандартное отклонение расстояния
- **p50** — медиана (50-й перцентиль) расстояния
- **warn** — порог «Внимание» (90-й перцентиль)
- **alert** — порог «Аномалия» (97-й перцентиль)

Рассчитанные пороги сохраняются в файлы:

- `models/ecn/thresholds.pkl` — пороги для ЭЦН
- `models/shgn/thresholds.pkl` — пороги для ШГН

Формат `thresholds.pkl` полностью соответствует расчёту в `training/clustering.ipynb` и имеет вид:

```python
{cluster_id: {"warn": float, "alert": float, "mean": float, "std": float, "p50": float}}
```

В продакшн-части системы дашборд при визуализации использует значения `warn`/`alert` для текущего кластера, извлекая их напрямую из `thresholds.pkl` для подсветки статуса отклонения (норма / внимание / аномалия).

## Запуск

Из корня репозитория (с установленным Poetry):

- `make run` — эмулятор потока данных + дашборд на http://127.0.0.1:8050
- `make run-fast` — то же с ускоренным эмулятором (пауза 0.5 с между строками)

В обычном режиме дашборд обновляется каждые 10 секунд, в быстром режиме — каждую 1 секунду. Два режима отображения:

- **По режимам** — карточки по скважинам: текущий режим (кластер), отклонение и временная шкала режимов за последние 24 часа.
- **По признакам** — выбор скважины и 9 графиков в реальном времени по каждому признаку (УС центр/периф., газ, температура, вода и т.д.).
